const tex = document.querySelector(".mainContainerHome");

document.addEventListener("mousemove",function (e) {
    this.querySelectorAll(".mainContainerHome").forEach((element) => {
        console.log(element)
      });
})

